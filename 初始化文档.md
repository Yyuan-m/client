# 豪华租车用户端 Vue3 官网项目 AI 提示词合集

> 项目背景：基于已有的「LUXURY CAR 大圣玩车后台管理系统」配套开发**用户端官网**，技术栈固定 Vue3 + Vite + Element Plus，视觉风格对标苹果官网：极简留白、低饱和高级配色、大图文区块、轻量化动效、干净无冗余元素，整体高端轻奢汽车租赁质感。
>
> 提示词分 5 大类：【项目整体需求提示词】【页面开发专用提示词】【组件封装提示词】【样式主题配置提示词】【接口对接 & 工具封装提示词】，可单独复制给到 AI 生成代码 / 文档 / 页面。

## 一、项目整体搭建通用提示词（新建项目初始化专用）

### 提示词 1：项目初始化 + 工程配置生成

```
需求：搭建大圣玩车用户端官网，配套后端为LUXURY CAR豪华租车后台管理系统，输出完整可运行Vue3工程。
技术栈硬性要求：Vue3 + Vite5 + Element Plus + Vue Router4 + Pinia + Axios + Sass + ECharts + Dayjs
视觉规范：整体参考苹果官网设计风格，极简留白、大间距布局、柔和阴影、低饱和配色、轻量化过渡动画、无复杂装饰，高端轻奢汽车质感
工程配置要求：
1. vite.config配置@路径别名，全局SCSS变量自动注入，开发端口3000，代理/api转发至后端192.168.5.102:8088，分包优化打包产物
2. Pinia全局状态：用户登录状态、租车购物车、筛选条件、全局弹窗、主题缓存
3. Vue Router：首页/车辆列表/车辆详情/租车下单/个人中心/订单列表/订单详情/关于我们/联系客服路由，区分游客路由、登录鉴权路由，未登录访问个人中心/下单页自动跳转登录
4. Axios统一请求封装：继承后台接口规范，携带token、401过期跳转登录、统一错误拦截、上传下载方法封装
5. Element Plus全局按需引入，自定义主题色适配豪华租车品牌，全局message/notification挂载
6. 公共工具库：时间格式化、金额计算、租车天数计算、本地存储封装、图片懒加载工具
7. 目录分层：views页面、components公共组件、api接口模块、stores状态、utils工具、assets静态资源、router路由、styles全局样式
输出内容：完整项目目录结构、package.json依赖、vite.config.js、main.js全局入口、路由基础代码、请求封装request.js、全局scss变量文件
```

### 提示词 2：全局设计规范 & 主题配置

```
基于Vue3+Element Plus开发豪华租车用户官网，风格对标苹果官网极简简约风，输出全局样式规范+Element Plus主题配置代码
设计硬性标准：
1. 布局：大量留白，区块间距统一，内容居中限定最大宽度，不铺满全屏，分层柔和阴影，圆角统一偏小
2. 配色：主色低饱和深海蓝（高端汽车调性），辅助色浅金，中性色纯白/浅灰/深灰，禁用高饱和刺眼颜色；支持浅色模式（默认），预留暗色模式扩展
3. 字体：无衬线字体，层级清晰，标题粗体大号、正文纤细常规，行高宽松
4. 动效：苹果同款轻量化过渡，页面切换淡入、hover缓慢上浮、弹窗平滑弹出，无闪烁、无夸张动画
5. 组件规范：按钮极简（填充/线框两种，无多余图标装饰）、输入框无边框浅底设计、卡片无厚重边框，仅底部细微阴影
输出：全局scss变量文件（颜色、圆角、间距、字体、阴影、动画）、Element Plus自定义主题覆盖代码、全局重置样式、页面通用布局容器组件
```

## 二、单页面开发专用提示词（逐个页面生成代码）

### 提示词 3：首页 Home 页面

```
开发豪华租车用户端官网首页Vue页面，Vue3+Element Plus，苹果官网极简简约风格，页面适配PC端，分区块从上至下布局：
1. 顶部全屏轮播区块：调用后台轮播图接口/api/carousel/active，高清大图、淡入切换动画，无花哨指示器，底部简洁文字介绍
2. 品牌简介区块：左右分栏，左侧极简文字、右侧高端跑车静态图，大量留白
3. 热门车型推荐区块：卡片布局，每台车辆卡片遵循苹果极简设计，仅展示车型图、名称、日租价格、租车按钮，hover轻微上浮，点击跳转车辆详情；支持横向滚动
4. 服务优势区块：4列均等卡片，图标+短句文案，留白充足，无分割线
5. 客户评价区块：横向滚动用户真实评价，浅灰色背景区分区块
6. 活动优惠券区块：简约横幅，展示后台营销优惠券，点击跳转车型列表
7. 底部预约咨询区块：浅蓝底色，预约表单（姓名、手机号、意向车型、取车日期），简洁提交按钮
8. 页脚：导航链接、客服电话、企业地址、版权信息
功能要求：所有数据通过axios请求后台对应接口，页面适配大屏/中屏，使用ECharts预留数据统计模块，添加页面滚动渐显动画，统一封装公共轮播、卡片组件，代码结构清晰拆分子组件
```

### 提示词 4：车辆列表 & 筛选页面

```
开发租车车辆列表页Vue3页面，Element Plus，苹果极简风，PC端适配
页面结构：
1. 顶部筛选栏：简约下拉筛选（车型分类：轿车/SUV/跑车/商务/新能源、取车城市、租期、价格区间、排序方式），无复杂边框，浅底色筛选面板
2. 左侧侧边筛选面板：收起/展开功能，多条件复选筛选，右侧车辆卡片网格布局
3. 车辆卡片统一规范：苹果极简风格，大图优先，仅展示车型、日租金、车辆状态、简单标签，hover轻微上浮，加入购物车/立即租车两个极简按钮
4. 分页：底部简约分页组件，无多余样式
5. 空状态：无匹配车辆时展示极简空白提示页，附带重置筛选按钮
逻辑要求：
1. 筛选条件存入Pinia全局状态，切换筛选实时请求车辆列表接口/api/car
2. 车辆支持加入租车购物车，购物车数据持久化localStorage
3. 点击卡片跳转车辆详情页
输出完整vue单文件，拆分筛选、车辆卡片子组件，添加加载占位骨架屏
```

### 提示词 5：车辆详情页面

```
开发豪华租车车辆详情Vue页面，Vue3+Element Plus，苹果官网极简高级风格
页面区块：
1. 顶部大图预览区：多图横向滚动预览，支持点击放大看图，无多余边框
2. 核心信息区：左侧车辆大图，右侧极简文字信息（车型全称、日租价格、租期规则、车辆配置标签、取还车说明），立即租车、加入收藏按钮
3. 车辆完整配置区块：分栏展示动力、内饰、安全配置，极简表格样式
4. 租期选择模块：日期选择器（dayjs计算租车总天数、实时更新总价、押金金额），支持最少/最长租期限制（读取后台租赁规则配置）
5. 须知区块：押金政策、逾期规则、取消政策，折叠面板简约展示
6. 底部推荐同系列车型横向卡片
功能：
1. 收藏车辆存入Pinia，持久化本地存储
2. 选择取还车日期实时计算租金、押金、总应付金额
3. 点击立即租车校验登录状态，未登录跳转登录页，已登录跳下单确认页
```

### 提示词 6：租车下单 & 结算页面

```
开发租车订单结算下单页面，Vue3+Element Plus，苹果极简留白设计
页面模块：
1. 订单商品概览：顶部展示所选车辆、租期、取还车时间、基础租金
2. 取还车信息表单：城市选择、取车门店、取车时间、联系人、联系电话、身份证上传组件（FileUploader，限制图片格式大小）
3. 优惠券选择：下拉选择可用优惠券，实时抵扣金额，自动更新应付总价
4. 费用明细：极简列表展示租金、押金、优惠券抵扣、合计金额
5. 底部固定结算栏：总金额、提交订单按钮
逻辑规则：
1. 表单校验：手机号、身份证、取还车时间必填校验
2. 优惠券接口请求后台/api/coupon，仅展示用户可用未过期券
3. 提交订单调用/api/order创建订单，创建成功跳转支付/订单详情页
4. 未登录用户拦截跳转登录，登录后返回当前下单页面
5. 全局加载状态、提交防重复点击
```

### 提示词 7：登录 / 注册 / 忘记密码页面

```
开发用户端登录、注册、忘记密码三套Vue页面，Element Plus，苹果极简纯净风格，纯白背景，居中表单，弱化边框、柔和输入框阴影
通用规范：
1. 页面居中布局，左右分栏可选，左侧简约汽车宣传图，右侧表单卡片，无厚重边框
2. 表单元素极简：浅底输入框、细线按钮，主色填充登录按钮，线框切换账号按钮
登录页：账号密码登录、记住密码、跳转注册/忘记密码；登录成功存储token至Pinia，跳转来源页面
注册页：手机号、验证码、密码、确认密码表单校验，注册完成自动跳转登录
忘记密码页：两步流程（手机号验证→重置新密码），完成提示成功跳转登录
接口对接：调用后台/api/auth登录、注册、重置密码接口，统一错误提示
```

### 提示词 8：个人中心全套页面（订单 / 收藏 / 优惠券 / 个人信息）

```
开发用户端个人中心合集页面，Vue3+Element Plus，苹果极简风，侧边菜单布局
包含4个子tab页面：
1. 个人信息：头像上传、姓名、手机号修改、修改密码表单
2. 我的订单：分页展示全部订单，按状态筛选（待支付、租赁中、已完成、已取消、逾期），每个订单卡片展示车辆、租期、金额、操作按钮（支付、续租、取消订单、查看详情）
3. 我的收藏：收藏车辆卡片网格，支持取消收藏，空状态提示
4. 我的优惠券：可用/已过期tab，极简卡片展示优惠券规则
权限控制：未登录访问自动跳转登录页，token失效清空用户状态跳转登录，所有接口携带用户token鉴权，数据存入Pinia用户状态管理
```

### 提示词 9：关于我们 / 联系客服静态页面

```
开发用户端静态页面：关于我们、联系客服，Vue3+Element Plus，苹果官网大留白极简设计
关于我们：品牌发展简介、企业优势、线下门店分布图（静态图文）、团队介绍，大图文分块，无复杂装饰
联系客服：客服电话、在线咨询入口、线下门店地址列表、留言反馈表单（姓名、手机号、留言内容提交）
页面无复杂交互，侧重图文排版，统一全局间距、柔和阴影、轻量化文字层级
```

## 三、公共组件封装提示词（全局复用组件）

### 提示词 10：全局通用基础组件封装

```
基于Vue3+Element Plus封装豪华租车用户端全局公共组件，遵循苹果极简设计风格，全部可全局引入使用，输出每个组件完整vue代码：
需要封装组件清单：
1. AppHeader：网站顶部导航栏（logo、车型列表下拉、优惠券入口、个人中心、登录/注册按钮，滚动时导航栏渐变透明效果）
2. AppFooter：全局页脚导航组件
3. CarCard：统一车辆卡片组件（接收车辆信息props，包含收藏、租车按钮，hover动效）
4. PageSkeleton：页面加载骨架屏，适配车辆列表、详情、个人中心
5. ImagePreview：全局图片放大预览弹窗
6. DateRentPicker：租车专用日期选择器，内置租期计算、最小/最大租期限制、自动计算天数
7. CouponCard：优惠券通用卡片组件
8. EmptyTips：空数据全局提示组件（无车辆、无订单、无收藏）
9. BackTop：返回顶部极简悬浮按钮
组件要求：props完整类型校验、样式使用全局scss变量、无内联样式、动效统一轻量化、适配PC端
```

## 四、接口 & 工具封装提示词

### 提示词 11：API 接口模块全套封装

```
根据后台LUXURY CAR豪华租车后台接口文档，封装用户端Vue3项目全套axios api模块，技术使用Pinia+axios，规范如下：
1. 基础request.js统一请求拦截：自动携带token、401跳转登录、重复请求防抖、统一错误提示、上传文件封装
2. 按业务拆分api模块文件：
- auth.js：登录、注册、找回密码、获取用户信息、退出登录
- car.js：车辆列表筛选、车辆详情、车辆图片、车型分类
- order.js：创建订单、获取订单列表、订单详情、取消订单、支付订单
- coupon.js：可用优惠券列表、领取优惠券
- user.js：修改个人信息、上传头像、修改密码、收藏车辆、收藏列表
- carousel.js：首页轮播图
- system.js：网站配置、租赁规则、门店地址、公告
3. 所有接口请求函数增加注释，返回Promise，统一解构返回data，适配页面直接调用，输出完整api目录全部代码
```

### 提示词 12：通用工具函数 utils 封装

```
为Vue3豪华租车用户端编写全局工具函数库utils/index.js，配套dayjs使用，满足租车业务场景，函数清单：
1. storage封装：localStorage/sessionStorage增删改查，自动序列化JSON
2. 时间工具：格式化日期、计算两个日期间隔天数、获取未来日期、租期规则校验
3. 金额工具：金额千分位格式化、租金+押金总价计算、优惠券抵扣计算
4. 图片工具：图片懒加载、图片地址拼接、默认兜底图
5. 表单校验正则：手机号、身份证、密码通用校验规则
6. 权限工具：判断用户是否登录、清除登录全部缓存（token、用户信息、购物车）
7. 浏览器工具：复制链接、页面滚动、防抖节流函数
输出完整工具代码，附带每个函数使用注释
```

## 五、业务功能综合开发提示词（完整页面 + 逻辑一体生成）

### 提示词 13：购物车租车购物逻辑完整开发

```
在Vue3豪华租车官网中实现租车购物车完整业务逻辑，Element Plus、苹果极简风格，使用Pinia管理购物车状态：
需求功能：
1. 车辆详情/列表页点击加入购物车，重复车辆自动合并租期，弹窗极简成功提示
2. 导航栏购物车悬浮弹窗：展示已选车辆、租期、单价、合计金额，快速跳转结算
3. 购物车页面：批量勾选车辆、修改租期、删除单台车辆、清空购物车、批量结算下单
4. 购物车数据持久化localStorage，登录后同步后台用户购物车数据
5. 结算时校验车辆状态（已出租/维修车辆无法下单，弹出提示移除）
输出：Pinia购物车store代码、购物车页面完整vue组件、加入购物车全局弹窗组件、对应接口调用逻辑
```

### 提示词 14：页面动画 & 苹果官网滚动交互开发

```
为Vue3豪华租车用户官网添加苹果官网同款轻量化滚动交互效果，不使用重型动画库，纯CSS+原生JS实现：
1. 页面区块滚动渐显：进入视口才淡入上浮展示
2. 顶部导航栏滚动渐变：页面顶部时透明背景，向下滚动变为纯白固定导航
3. 卡片hover柔和上浮、阴影缓慢加深过渡动画
4. 路由页面切换淡入淡出过渡动画
5. 弹窗弹出平滑缩放动画，无生硬闪现
所有动画参数统一写入全局scss变量，可一键修改速度、透明度，不影响页面加载性能，输出全局动画样式+滚动监听hooks封装代码
```